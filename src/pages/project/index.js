/* global tw */
import React from 'react';
import styled from 'react-emotion';

import Layout from '../../components/Layout';

export default () => (
  <Layout>
    <p>This is project</p>
  </Layout>
);
